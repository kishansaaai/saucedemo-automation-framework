[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    smoke: quick smoke-test subset
    slow: known slow tests (e.g. performance_glitch_user flows)

